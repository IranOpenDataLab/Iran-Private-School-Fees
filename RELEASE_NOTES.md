# 📦 توضیحات آخرین به‌روزرسانی داده

> تاریخ انتشار (شمسی و میلادی) در **عنوان Release** درج می‌شود و به‌صورت خودکار از آخرین
> commit فایل `schools_data.csv` محاسبه می‌گردد.

## این نسخه شامل چیست؟

- استخراج کامل **۲۴٬۶۴۸ مدرسه غیردولتی** از `portal.mosharekatha.ir` و `my.mosharekatha.ir`
  — ۳۲ استان، ۵۵۸ منطقه، **همه مقاطع** (ابتدایی، ابتدایی استثنایی، متوسطه دوره اول،
  متوسطه دوره دوم شاخه‌های نظری / فنی و حرفه‌ای / کاردانش)، سال تحصیلی ۱۴۰۵-۱۴۰۶.
- `schools_data.csv` — **۴۲ ستون** پاکسازی‌شده؛ UTF-8 با BOM (مناسب اکسل).
- `schools_data.parquet` — همان ۴۲ ستون به‌علاوه `raw_detail` / `raw_licenses` /
  `raw_tuition_history` (در مجموع ۴۵ ستون؛ اشیاء تو در توی API به‌صورت رشته JSON).

## پاکسازی اعمال‌شده

- نام استان **بدون کد و پرانتز عددی**؛ در ستون منطقه **فقط نام ناحیه** (پرانتزِ متنیِ
  رسمی حفظ می‌شود).
- `confirm_tuition` از توکن‌های بک‌اند به **بولین** تبدیل شده؛ `process_status` به
  **برچسب فارسی دقیق UI داشبورد** (ستون `process_status_label`).
- ستون‌های شهریه **زمان‌چین** با الگوی `{سال}_{نوع}` (۱۴۰۳ ← ۱۴۰۴ ← ۱۴۰۵) به‌علاوه
  ستون‌های جمع سالانه `*_total` (= شهریه + فوق‌برنامه).
- مبالغ اعشاری خروجی سرور به **نزدیک‌ترین ریال** رند شدند تا `*_total` همیشه با مقادیر
  نمایش‌داده‌شده برابر باشد؛ ساعت فوق‌برنامهٔ کسری (مثل `7.5`) دست‌نخورده ماند.
- `stage` به‌صورت **اسم مقطع** (نگاشت کدها از بج و مجوز خود سایت تأیید شده؛ کد خام `stage_type_id` حذف شده).

## راستی‌آزمایی

`python validate_data.py` → **۵۰٬۷۱۹ بررسی، ۰ خطا** (سلامت JSONL، نرخ خطا،
فرمت پول، فرمت ساعت، اشکال چهارگانهٔ ستون‌های `*_total`).

## نحوه دریافت فایل‌ها

فایل‌های `schools_data.csv` و `schools_data.parquet` را می‌توان مستقیماً از همین
بخش **Assets** دانلود کرد. راهنمای کامل ستون‌ها در فایل `metadata.md` مخزن است:

- مخزن: https://github.com/IranOpenDataLab/Iran-Private-School-Fees
- سازمان: https://github.com/IranOpenDataLab
