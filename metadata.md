# متادیتای خروجی «استعلام شهریه مدارس غیردولتی»

- **سال تحصیلی:** ۱۴۰۵-۱۴۰۶ (ثابت، طبق فیلتر سایت)
- **تاریخ استخراج:** ۲۰۲۶/۰۹/۲۳
- **فایل‌ها:**
  - `schools_data.csv` — UTF-8 با BOM، مناسب اکسل، دقیقاً **۴۲ ستون**
  - `schools_data.parquet` — همان ۴۲ ستون **به‌علاوه ۳ ستون `raw_*`** (مجموعاً ۴۵ ستون؛ اشیاء تو در توی API به‌صورت رشته JSON)
  - فایل `schools_data.json` **حذف شد** و جایگزین آن Parquet شد (ساختار داده، اندازه کوچک‌تر، نوع‌دهی درست)
- **منبع داده:** `portal.mosharekatha.ir` (لیست مدارس) و `my.mosharekatha.ir` (جزئیات/مجوز/تاریخچه شهریه)
- **تعداد رکوردها:** ۲۴,۶۴۸ مدرسه (درخت استعلام: ۳۳ استان × ۷۸۵ منطقه × ۲ جنسیت = ۱٬۵۷۰ ترکیب — همه اجرا شد؛ داده در ۳۲ استان و ۵۵۸ ناحیه موجود است، **همه مقطع‌ها**)

> ⚠️ استخراج اولیه فقط مقطع ابتدایی را پوشش می‌داد (باگ فیلتر مقطع در UI سایت — بخش «مقطع» را ببینید). مجموعه فعلی با فیلتر بازِ مقطع (کدهای ۰ تا ۹۹) از نو جمع‌آوری شده و هر ۵ مقطع واقعی را شامل می‌شود.

---

## پاکسازی اعمال‌شده (نکات ۲-۱ تا۳ و«مقطع»)

این تبدیل‌ها هم در `clean_data.py` (اجرا روی دیتای فعلی: `python clean_data.py`) و هم در `phase_export` از `run_scraper.py` (برای هر اسکریپت آینده) اعمال می‌شوند:

| # | قاعده | قبل | بعد |
|---|---|---|---|
| ۲-۱ | حذف کد و پرانتزِ عددی از استان | `شهر تهران(11)` | `شهر تهران` |
| ۲-۲ | فقط نام ناحیه در منطقه | `تهران .منطقه 1(1101)` | `منطقه 1` |
| ۲-۳ | توکن‌های `confirm_tuition` → بولین | `1rdc58xq30.b1b` / `False` | `True` / `False` |
| ۲-۴ | کد `process_status` → متن فارسی (ستون `process_status_label`) | `4` | `تایید کارشناس مسئول منطقه` |
| ۲-۵ | ستون‌های شهریه زمان‌چین با اولویت سال و جداکننده `_` | `tuition_1405` | `1405_tuition` |
| ۲-۶ | جمع سالانه | — | `1405_total` = `1405_tuition` + `1405_extra_curricular` |
| ۳ | مقطع به‌صورت اسمی (کد خام حذف می‌شود) | `stage_type_id=11` | `stage=متوسطه دوره دوم شاخه نظری` |

> در قاعده ۲-۲ فقط **کدهای عددی** حذف می‌شوند؛ پرانتزِ *متنی* که بخشی از نام رسمی
> ناحیه است حفظ می‌شود — مثلاً `مغان (گرمی )` ← `مغان (گرمی)` (تفکیک شهر/بخش در
> نام‌های تکراری مثل نورآباد). چنین عناوینی در ۱۵ ناحیه دیده می‌شوند.

---

## توضیح ستون‌ها (۴۲ ستون، به ترتیب خروجی)

### شناسایی و موقعیت

| ستون | توضیح | منبع |
|---|---|---|
| `province` | نام استان **بدون کد** (پاکسازی ۲-۱) | سلسله‌مراتب مناطق |
| `district` | فقط نام ناحیه/منطقه (**پاکسازی ۲-۲**) | سلسله‌مراتب مناطق |
| `school_name` | نام مدرسه | API لیست مدارس |
| `school_id` | کد مدرسه در سامانه (۸ رقمی مثل `80054934`) | API لیست مدارس |
| `school_path` | شناسه مسیری مدرسه در API (مثل `IR2O2O8O3Opz`) — پایه ساخت لینک | API لیست مدارس |
| `school_url` | لینک مستقیم صفحه جزئیات (`https://my.mosharekatha.ir/school-details/{school_path}`) | ساخته‌شده |
| `gender` | `پسرانه` یا `دخترانه` (کد ۱=پسرانه، ۲=دخترانه) | API لیست مدارس |
| `stage` | **نام فارسی مقطع** — جدول نگاشت پایین را ببینید؛ کد خام `stage_type_id` عمداً از خروجی حذف شده | نگاشت تأییدشده (بند «مقطع») |
| `address` | آدرس مدرسه (در صورت نبود در لیست، از جزئیات تکمیل می‌شود) | لیست/جزئیات |

### مالکیت و مدیریت

| ستون | توضیح | منبع |
|---|---|---|
| `manager` | مدیر مدرسه | `school-detail/load` |
| `founder` | موسس مدرسه | `school-detail/load` |
| `education_office_zone` | اداره آموزش و پرورش ناحیه/منطقه | `school-detail/load` |
| `education_office_province` | اداره کل آموزش و پرورش استان | `school-detail/load` |

### مجوز

| ستون | توضیح | منبع |
|---|---|---|
| `request_date` | تاریخ درخواست مجوز — میلادی ISO مثل `2023-11-19T08:41:34.673Z` | `school-licenses/load` |
| `request_date_jalali` | همون تاریخ به شمسی مثل `1402/08/28` | تبدیل در پردازش |
| `request_number` | **شماره درخواست** (مثل `12202875` — همون که UI نشان می‌دهد) | فیلد `CodeRequest` |
| `tracking_number` | شماره پیگیری رکورد مجوز (`RequestNumber`) — پایه لینک درگاه ملی مجوزها | `school-licenses/load` |
| `license_holder` | نام و نام خانوادگی مجوزدهنده | `FirstName`+`SurName` |
| `license_holder_national_code` | کد ملی مجوزدهنده | `NationalCode` |
| `license_operation` | نوع درخواست/عملیات مجوز (مثل «صدورمجوز راه اندازی مدرسه») | `OperationName` |
| `license_level` | مقطع مندرج در مجوز (مثل «ابتدایی دوره اول و دوم پسرانه») | `MainLevelName` |
| `license_province` | استان مندرج در مجوز | `StateName` |
| `license_zone` | ناحیه مندرج در مجوز (مثل «منطقۀ 01») | `AreaName` |
| `license_school_code` | کد مدرسه در رکورد مجوز (با `school_id` یکی است) | فیلد `Code` |
| `license_portal_url` | لینک مجوز در درگاه ملی مجوزها — قالب `https://qr.mojavez.ir/track/{tracking_number}` | ساخته‌شده |
| `licenses_count` | تعداد مجوزهای ثبت‌شده (۰ = «مجوزی برای این مدرسه ثبت نشده است») | شمارش آرایه |

### شهریه — نام‌گذاری ستون‌ها (قاعده ۲-۵ و ۲-۶)

> قالب: **`{سال}_{نوع}`** — سال اول، جداکننده زیرخط، مرتب‌سازی زمانی ۱۴۰۳ ← ۱۴۰۴ ← ۱۴۰۵.
> کلید سال در API ⇒ سال تحصیلی: `1403`⇒۱۴۰۳-۱۴۰۴، `1404`⇒۱۴۰۴-۱۴۰۵، `1405`⇒۱۴۰۵-۱۴۰۶.

| ستون | توضیح |
|---|---|
| `1403_tuition` / `1403_extra_curricular` / `1403_extra_hour` | شهریه مصوب / فوق‌برنامه / ساعت فوق‌برنامه سال ۱۴۰۳-۱۴۰۴ (رشته عددی) |
| `1403_total` | **جمع سال ۱۴۰۳** = `1403_tuition` + `1403_extra_curricular` (عدد یا خالی؛ قاعده ۲-۶: جزء خالی = ۰، جزء متنی ← خالی) |
| `1404_tuition` / `1404_extra_curricular` / `1404_extra_hour` | همان سه مقدار سال ۱۴۰۴-۱۴۰۵ |
| `1404_total` | جمع سال ۱۴۰۴ |
| `1405_tuition` / `1405_extra_curricular` / `1405_extra_hour` | استعلام سال ۱۴۰۵-۱۴۰۶ (از فاز لیست) |
| `1405_total` | جمع سال ۱۴۰۵ |
| `1405_final_tuition` | شهریه **نهایی‌شده** ۱۴۰۵-۱۴۰۶ از تاریخچهٔ صفحهٔ جزئیات (کلید `tuition` همراه `isfinal`)؛ فقط برای مدارس دارای فرایند تأییدشده **۱۳٬۱۴۰** مقدار دارد، بقیه خالی. بخش صحیح آن با `1405_tuition` عیناً برابر است (راستی‌آزمایی: ۱۳٬۱۳۹/۱۳٬۱۳۹). نکته: کلیدهای سال ۱۴۰۵ با سال‌های دیگر متفاوت است (`tuition`/`extracurricular`/`exta_hours` به‌جای `final_tuition`/…) |

**انواع مقادیر:** ستون‌های مقداری شهریه **رشته** می‌مانند (رقم صحیح انگلیسی یا متن سایت مثل «در انتظار تایید»؛ خالی → `NULL`). مبالغ اعشاری که سرور می‌فرستد (سریالایزر BigDecimal ،مثل `692022770.8333…` حاصل فرمول امتیازدهی) به **نزدیک‌ترین ریال** رند می‌شوند (`ROUND_HALF_UP`) و دنباله‌های `.0000…` حذف می‌گردند؛ اما `*_extra_hour` ممکن است **کسر ساعت واقعی** داشته باشد (`7.5` یعنی ۷٫۵ ساعت) و دست نمی‌خورد. ستون‌های `*_total` **عدد یا خالی**‌اند و از مقادیر رندشدهٔ همان ردیف ساخته می‌شوند؛ اگر جزئی متنی («در انتظار تایید») باشد `*_total` خالی می‌ماند (مبلغ هنوز معلوم نیست، صفر فرض نمی‌شود).

### شهریه — تایید و وضعیت فرایند (نکات ۲-۳ و ۲-۴)

| ستون | توضیح |
|---|---|
| `confirm_tuition` | **بولین.** مقادیر `1rdc58xq30.b1b` و `1rdc5g61nn.3e1` (دو توکن تأیید بک‌اند) ← `True`؛ `False`/خالی ← `False`. معنی UI: تأییدشده ⇔ شهریه به عدد نمایش داده می‌شود؛ تأییدنشده ⇔ «در انتظار تایید». |
| `process_status` | کد خام فرایند (رشته `'1'..'6'` یا خالی) — برای ارجاع/فیلتر |
| `process_status_label` | **متن فارسی دقیق ستون «وضعیت» داشبورد** — جدول پایین |

**نگاشت `process_status` ← مستقیماً از UI داشبورد رمزگشایی‌شده** (۲۰۲۶/۰۹/۲۳؛ با هفت جستجوی هدفمند نام مدرسه که هر کد و هر حالت تأیید را پوشش داد، مثلاً سریر راستی=`4`، مسیر دانش=`3`، سمیع دانش=`5`، افق زرین تیله=`2`، شهید مدافع حرم محمدرضا دهقان=`6`، بوستان دانش=`''`):

| `process_status` | `process_status_label` (متن UI) | توضیح فرایند |
|---|---|---|
| `` (خالی/`null`) | عدم ثبت درخواست شهریه | موسس هنوز درخواستی ثبت نکرده |
| `1` + `confirm_tuition=False` | ابلاغ شهریه - در انتظار تایید | شهریه ابلاغ شده ولی تأیید نهایی ندارد |
| `1` + `confirm_tuition=True` | ابلاغ شهریه | شهریه ابلاغ‌**و تأییدشده** (مبالغ عددی در UI) |
| `2` | تکمیل شده توسط موسس | درخواست توسط موسس تکمیل شده |
| `3` | در انتظار تکمیل موسس | درخواست ایجاد شده، در انتظار تکمیل موسس |
| `4` | تایید کارشناس مسئول منطقه | تأیید کارشناس ناحیه |
| `5` | رد شده توسط کارشناس مسئول منطقه | رد توسط کارشناس ناحیه |
| `6` | رد شده توسط کارشناس مسئول استان | رد توسط کارشناس استان |

> کد `۱` تنها کد وابسته به `confirm_tuition` است؛ بقیه کدها در همه ردیف‌ها فقط با `confirm=False` دیده شده‌اند. کدهای ناشناختهٔ آینده عیناً در `process_status_label` باقی می‌مانند تا دیده شوند.
> توزیع تعداد هر وضعیت و سایر آمار خودکار: `README.md` (با GitHub Action).

### مقطع — نگاشت تأییدشده کد ← اسم (نکته۳)

| `stage` (خروجی) | کد خام `stage_type_id` (حذف‌شده) | برچسب UI سایت |
|---|---|---|
| ابتدایی | `3` | دوره ابتدایی توصیفی |
| متوسطه دوره اول | `5` | دوره متوسطه اول |
| متوسطه دوره دوم شاخه نظری | `11` | متوسطه دوم - نظری |
| متوسطه دوره دوم شاخه فنی و حرفه‌ای | `12` | متوسطه دوم - هنرستان فنی |
| متوسطه دوره دوم شاخه کاردانش | `15` | متوسطه دوم - هنرستان کاردانش |
| ابتدایی استثنایی | `31` | ابتدايي استثناايی (بج صفحه جزئیات) + مجوز «ابتدایی دوره اول و دوم استثنایی» — **۸ مدرسه** |
| متوسطه دوره اول | `90`، `92` | بج سایت کد خام نشان می‌ده (دیکشنری ناقص)؛ از متن مجوز خود سایت: «متوسطه دوره اول …» — **۲ مدرسه** (مهر و ماه / مکتب الصادق) |

> **چرا کدها عوض شدند؟** درخواست API خود سایت فیلد `stage_id` را همیشه `[2,3,16,17]` می‌فرستد (حتی با انتخاب یک گزینه در dropdown — باگ سایت) و بک‌اند مستقیماً روی **کد ردیف‌ها** فیلتر می‌کند؛ چون کد ردیف ابتدایی `3` است، اسکریپت اولیه فقط ابتدایی را برمی‌گرداند. اسکریپت فعلی `stage_id` باز (`0..99`) می‌فرستد و همه ردیف‌ها را می‌گیرد. نگاشت بالا از بج‌های خود صفحه جزئیات مدرسه و جدول داشبورد تأیید شده است.

### ستون‌های `raw_*` (فقط در `schools_data.parquet`)

| ستون | توضیح |
|---|---|
| `raw_detail` | پاسخ خام `school-detail/load` |
| `raw_licenses` | آرایه کامل مجوزها (ستون‌های `license_*` فقط **اولین** مجوز را نشان می‌دهند) |
| `raw_tuition_history` | شیء کامل تاریخچه شهریه با کلیدهای ۱۴۰۳/۱۴۰۴/۱۴۰۵ به‌همراه فیلدهای خام مانند `obj_status` |

---

## نکات مهم

1. **واحد پول:** ریال؛ مقادیر عددی به‌صورت **رشته** (`"360000000"`) — کسرهای زیر-ریالِ خروجی سرور به صحیح رند شده‌اند — و جمع‌ها (`*_total`) استثنا و عددی‌اند.
2. **خالی‌های طبیعی:** `licenses_count=0` (مجوز ثبت نشده)، تاریخچه شهریه خالی، و `1405_final_tuition` خالی برای ۱۱٬۵۰۸ مدرسه‌ای که فرایند شهریه ۱۴۰۵شان ثبت/تأیید نشده — طبق UI سایت.
3. **تکرار:** هر مدرسه دقیقاً یک‌بار (حذف تکرار با `school_path`).
4. **پوشش و آمار زنده:** درصد پوشش مجوز/تاریخچه/جزئیات و شمارش وضعیت‌ها در `README.md` به‌صورت خودکار به‌روز می‌شود (`scripts/update_readme_stats.py`).
5. **اجرای مجدد:**
   - استخراج/به‌روزرسانی: `python run_scraper.py` (فازهای `list | details | export` با checkpoint در `checkpoint/`)
   - پاکسازی مستقل دیتای موجود: `python clean_data.py`
   - راستی‌آزمایی: `python validate_data.py`
   - آمار README: `python scripts/update_readme_stats.py`
6. **وابستگی‌ها:** `requests`, `pandas`, `pyarrow` (جزئیات نصب در `README.md`).
